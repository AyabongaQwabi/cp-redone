const MyComponent = () => {
  // Declare the variables
  const brevity = "brevity"
  const it = "it"
  const is = "is"
  const correct = "correct"
  const and = "and"

  // Some code that uses the declared variables
  console.log(brevity, it, is, correct, and)

  return (
    <div>
      <h1>Admin Page</h1>
    </div>
  )
}

export default MyComponent

